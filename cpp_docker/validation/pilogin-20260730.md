# pilogin 单 SIF 验证记录（2026-07-30）

## 验证范围

本记录验证同一个预编译 SIF 在 x86_64 Slurm 集群上的通用 PMI2 启动路径：

`srun --mpi=pmi2` → `apptainer exec --cleanenv` → SIF 内 OpenMPI 4.1.8。

pilogin 是验证站点，不对应单独的 Docker target、Pixi environment、镜像
tag 或 runner。验证工作区位于 `~/ysx/Agents/cpppipeline-portable`，日志
位于 `~/ysx/Agents/outputs`。

## 站点能力

- 架构：x86_64；
- Slurm：25.11.2；
- `srun --mpi=list`：包含 `pmi2`；
- Apptainer：1.5.2；
- 共享文件系统对全部计算节点可见。

## 最终制品

- Docker tag：`cpppipeline-dev:gxx12.3-openmpi4.1.8-pmi2`；
- Docker manifest-list ID：
  `sha256:3640b5add63167aa6a5919939f5139c6ca56b6288050e1a66935935009501b47`；
- Docker archive：376,257,536 bytes；
- Docker archive SHA256：
  `07c0277647a04d92533465a48c34ed9872d8c9a388cd0398b4492c8aef3c0304`；
- SIF：336,166,912 bytes；
- SIF SHA256：
  `0db2ada5eab77d243f49a998043e55a5ba620ac0ad93690817ffb4d7dae1591c`。

SIF checksum sidecar 与实际文件一致，`sha256sum --check` 返回 `OK`。

## 最终作业证据

| 操作 | Job ID | Slurm 状态 | 资源 | 节点 | 用时 |
|---|---:|---|---|---|---:|
| SIF 构建 | `60090091` | `COMPLETED 0:0` | 1 task，4 CPU | `cas008` | 7:38 |
| pipeline 编译 | `60090603` | `COMPLETED 0:0` | 1 task，4 CPU | `cas008` | 0:25 |
| 单节点 smoke | `60090644` | `COMPLETED 0:0` | 2 tasks，2 CPU | `cas008` | 0:10 |
| 双节点 smoke | `60090690` | `COMPLETED 0:0` | 4 tasks，独占 2 节点 | `cas235`,`cas305` | 0:13 |

双节点作业因 pilogin 的调度策略使用 `--exclusive`，Slurm 记录的
`AllocCPUS` 为 80；实际 `srun` 仍只启动 4 ranks，每节点 2 ranks。

对应日志：

- `cpp-sif-build-fix-60090091.out` / `.err`；
- `cpp-compile-fix-60090603.out` / `.err`；
- `cpp-mpi-smoke-1n-fix-60090644.out` / `.err`；
- `cpp-mpi-smoke-2n-60090690.out` / `.err`。

## 编译门槛

`60090603` 在 SIF 内报告：

- `mpicxx: Open MPI 4.1.8`；
- `MCA ess: pmi`；
- SIF hash、bind 与版本检查通过；
- 18 个 translation unit 全部重新编译并链接；
- `Fourier_Quad_Main` 大小为 958,888 bytes。

stderr 包含原有源码未使用参数/变量以及 Eigen 模板 warning，没有编译或
链接 error。

## MPI 与科学栈门槛

单节点 `60090644`：

- `rank 0 of 2 on cas008.pi.sjtu.edu.cn`；
- `rank 1 of 2 on cas008.pi.sjtu.edu.cn`；
- CFITSIO 4.0604、Eigen、LAPACK、FFTW 综合测试通过。

双节点 `60090690`：

- rank 0、1 位于 `cas235.pi.sjtu.edu.cn`；
- rank 2、3 位于 `cas305.pi.sjtu.edu.cn`；
- CFITSIO 4.0604、Eigen、LAPACK、FFTW 综合测试通过。

两个作业结束后，`scratch` 顶层条目数均为 0。stderr 中只有用户命名空间
使 OpenMPI `vader` 无法使用 CMA、转而选择其他 single-copy 机制的性能
提示。

## 失败门槛与修复

首轮 SIF `60088222` 和编译 `60088635` 成功，但 smoke `60088685` 在
`MPI_Init` 失败。该镜像包含 PMI2，却未构建 OpenMPI 的 Slurm 环境识别
组件。

同一镜像定义随后增加 `--with-slurm`，并以 `MCA schizo: slurm` 作为硬
断言；`--with-pmi=/opt/pmi2` 保持不变。失败 archive、SIF、sidecar 与
runner 使用带 `noslurm-60088685` 的名称保留。修复没有引入第二个镜像、
宿主 MPI 或宿主 `mpirun`。

## 结论与边界

最终单 SIF 已在 pilogin 完成构建、完整 cpp_Standard 编译、单节点 PMI2
和双节点 PMI2 验证。可移植支持契约是 x86_64 Linux、Slurm 暴露 PMI2、
Apptainer/Singularity 和跨节点共享文件系统。

这次结果不等于已验证 ARM、PBS/LSF、只提供 PMIx 的 Slurm，或
UCX/OFI/InfiniBand vendor fabric 加速。真实科学 pipeline 运行前仍需把
站点数据 bind 目标与 `LensingConfig.hpp` 中的科学路径配置对齐。
